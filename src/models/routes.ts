export enum Endpoints {
  Home = "/",
  Register = "/register",
  Landing = "/landing",
  AllModules = "/all-modules",
  CreateModule = "/create-module",
  UpdateModule = "/update-module",
  Profile = "/profile",
  SingleModule = "/module",
  Quiz = "/quiz",
}
